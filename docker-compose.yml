services:
  db:
    image: pgvector/pgvector:pg17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: shadpedia
      POSTGRES_PASSWORD: shadpedia
      POSTGRES_DB: shadpedia
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
